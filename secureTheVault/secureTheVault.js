// create a str variable:

let userMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination: "

// create three variables that contain the corresponding result of calculation using a arthmetic operator. Each must equal one of the three codes in combination.

const code1 = 10 + 5;
const code2 = 10 - 3;
const code3 = 5 * 3;

// create a dialogue box using html and a script tag

function showAlert(){
    alert(`${userMessage}${code1}-${code2}-${code3}`)
}

showAlert();
